# popcorndb (iOS / Swift)

A simple Swift app that displays movie data using **The Movie Database (TMDB)** API.

---

## 📋 Requirements
- Xcode 15 or newer  
- iOS 16+  

---

## ⚙️ Setup (only one step!)

1. Get a **free TMDB API key**  
   - Visit [https://www.themoviedb.org](https://www.themoviedb.org)  
   - Create a free account and generate an API key.

2. Open the following file in your project:

3. Replace the placeholder with your own TMDB API key:

```swift
>popcorndb>Core>Network>ApiConstants
// Add your TMDB key below and build the app.
enum APIConstants { //auth key
    // static let accessToken =  add your token here //V4 token
}
